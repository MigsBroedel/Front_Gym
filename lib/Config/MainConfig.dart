import 'package:flutter/material.dart';

class Mainconfig extends StatelessWidget {
  const Mainconfig({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Config"), backgroundColor: Colors.blueGrey,),
    );
  }
}
